<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
        <% if(!username){%>
          <form style="position: absolute; right: 350px" action="/login" method="POST"> 
            <label for="login" style="color: white;"> Enter Username:</label>
            <input type="text" id="login" name="username" placeholder="e.g Saïd">
            <button  type="submit" class="btn btn-primary">Log In</button>
          </form>
        <%} else {%>
          <div style="position:absolute; right: 350px">   
           <form action="/logout" method="POST">
            <label for="logout">Logged In As: <%=username%></label>
            <button id="logout" class="btn btn-primary" type="submit">Log Out</button>
          </form>
        </div>
        <%}%> 
      </div>
    </div>
  </nav>
</header>

